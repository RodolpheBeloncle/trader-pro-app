# =============================================================================
# Stock Analyzer - Backend Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# -----------------------------------------------------------------------------
# Application
# -----------------------------------------------------------------------------

# Enable debug mode (True for development, False for production)
DEBUG=False

# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Server configuration
HOST=0.0.0.0
PORT=8000

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# -----------------------------------------------------------------------------
# Saxo Bank API Configuration
# Get your keys at: https://www.developer.saxo/openapi/appmanagement
# -----------------------------------------------------------------------------

# Your Saxo application key (required for trading features)
SAXO_APP_KEY=your_app_key_here

# Your Saxo application secret (required for trading features)
SAXO_APP_SECRET=your_app_secret_here

# Environment: SIM (simulation/sandbox) or LIVE (production with real money)
# WARNING: LIVE mode will execute real trades!
SAXO_ENVIRONMENT=SIM

# OAuth2 Redirect URI - MUST be registered in your Saxo app settings
# For local development, use: http://localhost:5173
SAXO_REDIRECT_URI=http://localhost:5173

# -----------------------------------------------------------------------------
# Security
# -----------------------------------------------------------------------------

# Encryption key for storing OAuth tokens securely
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Session secret for cookie signing
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SESSION_SECRET=

# -----------------------------------------------------------------------------
# Data Storage
# -----------------------------------------------------------------------------

# Directory for data files (tokens, cache)
DATA_DIR=data

# File name for token storage
TOKENS_FILE=tokens.json

# File name for market presets
MARKETS_FILE=markets.json
